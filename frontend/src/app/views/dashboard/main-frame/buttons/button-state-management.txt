State Flow:

    app inits to description

    config button is in off animation mode
    
    cube button stops on logo and is disabled for single and double clicks

    chart button stops at locked

    clicking config first time routes to config-info, 
    cube button starts blinking

    clicking cube button stops blinking and routes to config/balance, double click is still disabled

    on cube-button-state "digitTwoStart" double click is permitted
